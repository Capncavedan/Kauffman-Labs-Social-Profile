How to run

First:
bundle install

Then:
create a file called 'config.rb' with your Twitter API credentials like so:

Twitter.configure do |config|
  config.consumer_key = 'CONSUMER_KEY'
  config.consumer_secret = 'SECRET'
  config.oauth_token = 'OAUTH_TOKEN'
  config.oauth_token_secret = 'MORE_SECRET'
end


then cd into the directory and run:

ruby ./social_profile.rb <twitterhandle>


It should open the info.html file for you.

If you just want to see a sample of the output, check here:
http://port80productions.com/ruby_midwest_hackathon/